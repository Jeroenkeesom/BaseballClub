{% extends 'base.html.twig' %}

{% block title %}Baseball Club!{% endblock %}

{% block body %}
    {{ parent() }}
<div class="container-fluid">
        <div class="row">
            <div class="col s12">
                <div class="card ">
                    <div class="card-content">
                        <span class="card-title">Played Game Overview </span>
                        <table class="striped highlight responsive-table bordered">
                            <thead>
                            <tr>
                                <th>Player</th>
                                <th>IP%</th>
                                <th>GP#</th>
                                <th>GPr</th>
                                <th>IP#</th>
                                <th>IA#</th>
                                <th>TR%</th>
                                <th>I2T</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for key in calculatedPlayerResult %}
                            <tr>
                                <td>{{ key.player.firstName }} #{{ key.player.shirtNumber }}</td>
                                <td>{{ key.ips }} </td>
                                <td>{{ key.gp }}</td>
                                <td>{{ key.gpr }}</td>
                                <td>{{ key.ipn }}</td>
                                <td>{{ key.ipa }}</td>
                                <td>{{ key.tr }}</td>
                                <td>{{ key.i2t }}</td>
                            </tr>
                            {% endfor %}
                            </tbody>
                        </table>

                    </div>
                </div>
            </div>
        </div>
</div>
{% endblock %}
